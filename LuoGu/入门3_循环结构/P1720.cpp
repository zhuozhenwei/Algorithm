#include <bits/stdc++.h>
using namespace std;

int main()
{
    double n;
    cin >> n;
    cout << fixed << setprecision(2) << (pow(((1 + sqrt(5)) / 2), n) - pow(((1 - sqrt(5)) / 2), n)) / sqrt(5);
    return 0;
}